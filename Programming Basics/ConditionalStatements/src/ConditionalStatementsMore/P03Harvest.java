package ConditionalStatementsMore;

import java.util.Scanner;

public class P03Harvest {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int vineYardM2 = Integer.parseInt(scanner.nextLine());
        double grapeOneM2 = Double.parseDouble(scanner.nextLine());
        int wineLNeeded = Integer.parseInt(scanner.nextLine());
        int workersCount = Integer.parseInt(scanner.nextLine());

    }
}
